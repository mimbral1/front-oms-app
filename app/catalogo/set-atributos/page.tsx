import SetAtributosView from "@/features/catalogo/pages/set-atributos/SetAtributosView";

export const metadata = {
    title: "Set de atributos",
    description: "Sets de atributos del catálogo",
};

export default function SetAtributosPage() {
    return <SetAtributosView />;
}
